import React, { Component } from 'react';
import { View, Text } from 'react-native';
import {SliceMenu} from '../Router'

export default class HOME extends Component {
  render() {
    return (
        <SliceMenu/>
    )
  }
}
